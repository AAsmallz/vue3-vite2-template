import { createBuildOptions } from './build/index';
import { createPluginsOptions } from './plugins/index';
import { createServerOptions } from './server/index';

export const config = {
	createBuildOptions,
	createPluginsOptions,
	createServerOptions
}
